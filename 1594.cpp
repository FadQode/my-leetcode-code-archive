# include <iostream>
# include <vector>
# include <algorithm>

class Solution {
public:
    int maxProductPath(vector<vector<int>>& grid) {
        
        for(vector<int> x: grid){
          find(arr.begin(), arr.end(), 'a') != arr.end()
        }
    }
};